# Budget

# Enforced by the outer CLI; command-line --max/--timeout override these.
max_iterations: 20
max_seconds: 3600

# Worker guidance, not a sandbox. Host permissions remain authoritative.
stop_on_decision: true
allow_new_deps: false
allow_mcp_install: false
