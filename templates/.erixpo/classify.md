# Classify

ts:
repo_class: unknown
request_class:
surface: none
ui_change: none
# none | create | relanguage | retoken | recompose | reflow | remotion | new-screen | consistency
capabilities:
isolation: ask
ceremony: standard
jobs:
  - _request_class_: _one-line intent_
evidence:
  - _quoted file or fact that justified repo_class and surface_

_Fill on the first non-trivial /erixpo job. `ts` is ISO-8601. `jobs` is an ordered queue; `request_class` is the first remaining job. Delete these italics after the first write._
