# Constitution

_Fill during init (describe what exists; do not invent) or as slice 0 of new work, before product pixels. Delete this italic line._

surface:                 # web | ios | android | windows | macos | desktop | cli | script | tui | embedded | notes | none
layout:
  source:                # e.g. Sources/, src/, app/, .
  tests:                 # e.g. Tests/, tests/, .
  resources:             # e.g. Resources/, public/, n/a
  theme:                 # e.g. Assets/Theme.swift, src/theme.css, n/a
theme_file:              # native theme path / CSS variables path / none
test_dir:
test_runner:             # pytest, XCTest, gradle test, cargo test, none-yet
scaffold_cmd:            # command actually run, or "already existed"
check:                   # real command; echo / exit 0 / true forbidden
install_in_worktree:     # npm i, xcodebuild -resolvePackageDependencies, uv sync, none
how_to_add_a_screen:     # or n/a
how_to_add_a_test:
do_not:
  -
