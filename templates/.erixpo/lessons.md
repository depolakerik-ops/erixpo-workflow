# Lessons

Fresh workers have no memory. Anything the loop got wrong twice belongs here
so the next iteration does not repeat it. Keep entries short.

## Build
- (example) `npm test` needs the dev server stopped first

## Product
- (example) empty cart must render the empty state, not a spinner forever

## Do not
- Do not paste stack traces. Write the rule the next worker should follow.
