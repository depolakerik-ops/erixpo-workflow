---
description: Start or continue erixpo. Routes init, auto, feature, fix, review, docs, work, learn, search, ui, uninstall, update from one command.
argument-hint: "[request]"
---

Write `.erixpo/classify.md` (erixpo skill, references/classify.md), then run the **erixpo** skill as the router.

User request:
$ARGUMENTS

If `$ARGUMENTS` is empty, inspect the repo (init if no AGENTS.md, auto if an approved plan is waiting, otherwise one question).
