---
description: Start or continue erixpo. Routes init, new, feature, fix, review, auto, work, learn, search from one command.
argument-hint: "[request]"
---

Run the **erixpo** skill as the router.

User request:
$ARGUMENTS

If `$ARGUMENTS` is empty, inspect the repo and continue the correct flow (init if no AGENTS.md, auto if an approved plan is waiting, otherwise ask what they want to do — one question).
