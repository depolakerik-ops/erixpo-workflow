---
description: Start or continue erixpo. Routes init, auto, feature, fix, review, docs, work, learn, search, ui, uninstall, update from one command.
argument-hint: "[request]"
---

Run the **erixpo** skill as the router. Follow its classification and ceremony rules, including one-shot work and pack-maintenance exceptions; do not create project ceremony files before determining the request.

User request:
$ARGUMENTS

If `$ARGUMENTS` is empty, inspect the repo (init if no populated .erixpo/PROFILE.md for this project, auto if an approved plan is waiting, otherwise one question).
